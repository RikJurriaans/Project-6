module 'Animation'

# Hier moet ik nog even over denken.

# animate = ->
#     console.log 'test'

# A.animation = (object, properties) ->
#     _.bindAll(_.bind(object, properties), animate)

# A.animation = (object, properties) -> 
    # _.bind(animate, object, properties)
